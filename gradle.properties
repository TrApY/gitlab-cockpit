group = dev.jota.gitlabcockpit
version = 0.9.0

pluginRepositoryUrl = https://github.com/TrApY/gitlab-cockpit

# Baseline = plataforma contra la que se compila (2025.2). Declarar menos sería mentir:
# los APIs usados podrían no existir. Se bajará (junto con intellijIdea() en build.gradle.kts)
# cuando sepamos la versión real del IDE de JoTa en Europcar.
pluginSinceBuild = 252

# Opt-out flag for bundling Kotlin standard library -> https://jb.gg/intellij-platform-kotlin-stdlib
kotlin.stdlib.default.dependency = false

# Enable Gradle Configuration Cache -> https://docs.gradle.org/current/userguide/configuration_cache.html
org.gradle.configuration-cache = true

# Enable Gradle Build Cache -> https://docs.gradle.org/current/userguide/build_cache.html
org.gradle.caching = true
